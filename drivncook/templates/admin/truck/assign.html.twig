{% extends 'base.html.twig' %}

{% block title %}Gestion des camions{% endblock %}

{% block body %}

    <div class="container mt-5">

        {{ form_start(form) }}
        
            {{ form_widget(form) }}
 
            <div class="d-flex mt-4 justify-content-end">
                <a href="{{ path('admin_truck_show') }}" class="btn btn-primary mr-auto">Retour</a>
                <button class="btn btn-success" type="submit">Assigner le camion</button>    
            </div>
        
        {{ form_end(form) }}

    </div>
    
{% endblock %}

{% block scripts %}
    {{ parent() }}
{% endblock %}

