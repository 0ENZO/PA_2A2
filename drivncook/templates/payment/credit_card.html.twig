{% extends "base.html.twig" %}

{% block title %} Ajouter une CB {% endblock %}

{% block body %}

    <div class="container">
        {{ form_start(form) }}
            {{ form_widget(form) }}
        {{ form_end((form)) }}
    </div>

    {{ dump(user) }}
    {{ dump(credit_card) }}

{% endblock %}